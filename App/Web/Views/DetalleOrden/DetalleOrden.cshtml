@model List<Web.ViewModel.ViewModelDetalle>

<div class="table-responsive-sm">
    @*Accion, nombre de controlador*@

    <table class="table table-striped">
        @if (Model != null)
        {
            <tr>
                <th>
                    Producto
                </th>
                <th class="center">
                    Cantidad
                </th>
                <th class="right">
                    Precio
                </th>
                <th class="right">
                    SubTotal
                </th>
                <th>
                    Opciones
                </th>
            </tr>

            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.HiddenFor(modelItem => item.Producto.id)
                        @Html.DisplayFor(modelItem => item.Producto.nombre)
                    </td>
                    <td class="center" style="width:1.5em">
                        @Html.EditorFor(modelItem => item.cantidad, new { htmlAttributes = new { @class = "form-control changeCantidad", data_productoid = item.Producto.id, onchange = "updateCantidad(this)" } })
                    </td>
                    <td class="right">

                        @Html.DisplayFor(modelItem => item.precio)
                    </td>

                    <td class="right">
                        @Html.DisplayFor(modelItem => item.SubTotal)
                    </td>
                    <td>
                        @Ajax.ActionLink("Eliminar",//Texto para el usuario
                             "eliminarProducto",//Acción a llamar
                             "DetalleOrden",
                             new
                             {
                                 idProducto = item.idProd
                             },//Parámetros a enviar
                             new AjaxOptions
                             {
                                 UpdateTargetId = "resultadoDetalle",
                                 InsertionMode = InsertionMode.Replace,
                                 HttpMethod = "GET",
                                 OnComplete = "updateCantLibros()"
                             }, new { @class = "btn btn-primary" }
                             )

                    </td>
                </tr>
            }
        }
    </table>

</div>
@if (Model != null)
{

    <div class="row">
        <div class="col-lg-5 col-sm-5"></div>
        <div class="col-lg-7 col-sm-5 ml-auto">
            <table class="table table-clear">
                <tbody>
                    <tr>
                        <td class="left">
                            <strong>Total</strong>
                        </td>
                        <td class="right">
                            <strong>₡ @(((decimal)Web.ViewModel.Carrito.Instancia.GetTotal()))</strong>
                        </td>
                    </tr>
                </tbody>
            </table>

        </div>
    </div>
}